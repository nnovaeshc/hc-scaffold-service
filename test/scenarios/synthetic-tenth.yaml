name: synthetic-tenth
description: Synthetic tenth template, checking it completes unseen
compare: true
tier: 3
type: interview
feature: schema-walk
stub_scenario: default
prompt: /hc-scaffold-service Create a component using synthetic-tenth-template named data-processor
compare_prompt: Create a component using synthetic-tenth-template named data-processor
expected_output: The assistant fetches the synthetic template entity, does not report a constraint violation for the compliant name, completes without error, and issues catalog queries that include fields and limit.
expectations:
  - tool_called: get-catalog-entity
  - constraint_violation_reported: false
  - completion: true
  - catalog_queries_have_fields_and_limit: true
