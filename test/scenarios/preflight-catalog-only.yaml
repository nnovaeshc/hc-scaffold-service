name: preflight-catalog-only
description: Preflight failure - catalog capability but no scaffolder
compare: true
tier: 1
type: preflight
feature: fail-fast
stub_scenario: catalog_only
prompt: /hc-scaffold-service Create a new service
compare_prompt: Create a new service
expected_output: The assistant reports a configuration failure immediately, asks no questions, and never calls execute-template.
expectations:
  - question_count: 0
  - tool_not_called: execute-template
  - config_failure_message: true
