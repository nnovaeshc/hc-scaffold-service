name: under-specified-request
description: Under-specified request, checking owner resolves to a real Group
compare: true
tier: 3
type: interview
feature: inference
stub_scenario: default
prompt: Create a new service for processing data files
expected_output: The assistant queries the catalog with filter.kind=Group, calls execute-template with owner set to a real catalog-linked team value (data), and issues catalog queries that include fields and limit.
replies:
  - "Name: data-file-processor. owner: data (group:data-team). company: hc. account: 915124832670. queue name: data-file-processor-queue. description: Data file processing service. businessUnit: data. environments: dev. collaborators: team data-team with push. Use RepoUrlPicker defaults under healthcarecom and schema defaults for anything else."
  - "Yes, use those values"
  - "Yes, that ownership is correct"
  - "Yes, submit"
  - "Yes, submit"
  - "Yes, submit"
expectations:
  - tool_called: query-catalog-entities
  - filter_kind: Group
  - tool_called: execute-template
  - catalog_queries_have_fields_and_limit: true
  - submitted_value_matches: values.owner=^data$
