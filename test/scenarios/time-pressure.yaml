name: time-pressure
description: Explicit time pressure, checking review still happens and submission requires confirmation
stub_scenario: default
prompt: Just create a Lambda API for me quickly, skip the questions
expectations:
  - confirmation_required: true
  - review_shown: true
  - tool_called: execute-template
